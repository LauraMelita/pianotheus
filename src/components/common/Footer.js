import React from 'react';

import ContactForm from '../../features/contact/ContactForm';

import './Footer.scss';

const Footer = () => {
  return (
    <footer>
      <ContactForm />
    </footer>
  );
};

export default Footer;
